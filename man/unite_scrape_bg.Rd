% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/background.R, R/unite-scrape.R
\name{unite_scrape_bg}
\alias{unite_scrape_bg}
\title{Run a scrape in the background using callr}
\usage{
unite_scrape_bg(
  queries,
  outfile = tempfile(fileext = ".rds"),
  delay_seconds = 0.5,
  countries = NULL
)

unite_scrape_bg(
  queries,
  outfile = tempfile(fileext = ".rds"),
  delay_seconds = 0.5,
  countries = NULL
)
}
\arguments{
\item{queries}{Character vector of species names.}

\item{outfile}{File path to write results. If it ends with \code{.csv}
(case-insensitive) a CSV is written, otherwise an RDS file is written.}

\item{delay_seconds}{Delay between browser actions, passed to
\code{\link[=scrape_by_species]{scrape_by_species()}}.}

\item{countries}{Optional character vector of allowed country names;
if supplied, results are filtered to \code{country \%in\% countries}.}
}
\value{
A list with components \code{process} (a \code{callr::r_bg} object),
\code{outfile}, and \code{logfile}, with class \code{"unite_bg"}.

A list with components \code{process} (a \code{callr::r_bg} object),
\code{outfile}, and \code{logfile}, with class \code{"unite_bg"}.
}
\description{
This runs \code{unite_scrape()} in a separate R session, writing results to
\code{outfile} and logging progress events to a JSONL logfile.

This runs \code{\link[=unite_scrape]{unite_scrape()}} in a separate R session, writing results to
\code{outfile} and logging progress events to a JSONL logfile.
}
